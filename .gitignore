# Terraform state files (contain sensitive data)
*.tfstate
*.tfstate.*
.terraform/

# Terraform variables & backend configs (may contain secrets)
*.tfvars
backend-config.tfvars

# Local execution files (plans, override files)
*.tfplan
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Crash log files
crash.log

# Sensitive data (SSH keys, credentials)
*.pem
*.key
id_rsa
id_rsa.pub

# IDE-specific files
.vscode/
.idea/

# OS-specific files
.DS_Store
Thumbs.db

# Ansible files (if Ansible is in your repo)
ansible/hosts
ansible/.ansible.cfg
